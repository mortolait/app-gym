import { TypeTransactionPipe } from './type-transaction.pipe';

describe('TypeTransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeTransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
