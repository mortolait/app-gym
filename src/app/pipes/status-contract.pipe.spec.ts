import { StatusContractPipe } from './status-contract.pipe';

describe('StatusContractPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusContractPipe();
    expect(pipe).toBeTruthy();
  });
});
