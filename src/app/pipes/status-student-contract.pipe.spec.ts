import { StatusStudentContractPipe } from './status-student-contract.pipe';

describe('StatusStudentContractPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusStudentContractPipe();
    expect(pipe).toBeTruthy();
  });
});
