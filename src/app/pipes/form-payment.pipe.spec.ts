import { FormPaymentPipe } from './form-payment.pipe';

describe('FormPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
